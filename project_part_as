
.cart-content {
    height: 100%;
    display: flex;
    flex-direction: column;
    min-height: 0;
    background: #141414;
    position: relative;
    z-index: 2;
    pointer-events: auto;
}

.cart-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 24px;
    border-bottom: 1px solid var(--border-color);
    flex-shrink: 0;
    background: #141414;
}

.cart-header h3 {
    font-size: 24px;
    font-weight: 700;
}

.cart-close {
    background: none;
    border: none;
    color: var(--white);
    font-size: 24px;
    cursor: pointer;
    transition: var(--transition);
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}
.cart-close:hover {
    color: var(--primary-color);
}

.cart-items {
    flex: 1;
    overflow-y: auto;
    overflow-x: hidden;
    padding: 24px;
    min-height: 0;
    overscroll-behavior: contain;
    -webkit-overflow-scrolling: touch;
    touch-action: pan-y;
}

.empty-cart {
    text-align: center;
    color: var(--text-light);
    font-size: 18px;
    padding: 40px 0;
}

.cart-item {
    display: flex;
    gap: 16px;
    padding: 16px;
    background-color: #1c1c1c;
    border: 1px solid rgba(255, 255, 255, 0.07);
    border-radius: 12px;
    margin-bottom: 16px;
}

.cart-item-image {
    width: 80px;
    height: 80px;
    background-color: var(--bg-card-hover);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 32px;
    color: var(--text-light);
}

.cart-item-details {
    flex: 1;
}

.cart-item-name {
    font-weight: 600;
    margin-bottom: 8px;
}

.cart-item-price {
    color: var(--primary-color);
    font-weight: 700;
}

.cart-item-quantity {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-top: 8px;
}

.quantity-btn {
    width: 28px;
    height: 28px;
    border: 1px solid var(--border-color);
    background-color: rgba(255, 255, 255, 0.05);
    color: var(--white);
    border-radius: 6px;
    cursor: pointer;
    transition: var(--transition);
}

.quantity-btn:hover {
    background-color: var(--primary-color);
    border-color: var(--primary-color);
    transform: scale(1.05);
}

.quantity-btn:active {
    transform: scale(0.95);
}

.cart-item-remove {
    background: none;
    border: none;
    color: var(--error-color);
    cursor: pointer;
    font-size: 18px;
    transition: var(--transition);
    padding: 4px;
    border-radius: 4px;
}

.cart-item-remove:hover {
    color: var(--error-color);
    background-color: rgba(244, 67, 54, 0.1);
    transform: scale(1.1);
}

.cart-item-remove:active {
    transform: scale(0.95);
}

.cart-footer {
    padding: 24px;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    flex-shrink: 0;
    background-color: #111111;
}

.cart-total {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 16px;
}

.cart-total span:last-child {
    color: var(--primary-color);
}

@media (max-width: 360px) {
    .nav-brand a {
        font-size: 26px;
    }
}


==============================================
FILE: ./frontend/css/responsive.css
==============================================
/* Responsive Styles */

@media (max-width: 1024px) {
    .hero .container {
        grid-template-columns: 1fr;
        text-align: center;
    }
    
    .hero-buttons {
        justify-content: center;
    }
    
    .hero-badges {
        justify-content: center;
    }
    
    .hero-image {
        margin-top: 40px;
    }
    
    .hero-title {
        font-size: 56px;
    }
    
    .section-title {
        font-size: 36px;
    }
    
    .promo-content h2 {
        font-size: 36px;
    }
}

@media (max-width: 768px) {
    .nav-menu {
        position: fixed;
        top: 0;
        right: -100%;
        width: 280px;
        height: 100vh;
        background-color: var(--bg-card);
        flex-direction: column;
        padding: 80px 24px;
        gap: 24px;
        transition: var(--transition);
        box-shadow: -4px 0 20px rgba(0, 0, 0, 0.5);
    }
    
    .nav-menu.active {
        right: 0;
    }
    
    .nav-actions .btn:not(.nav-login-btn) {
        display: none;
    }

    .nav-actions {
        gap: 8px;
        min-width: 0;
    }

    .nav-login-btn {
        padding: 10px 12px;
        font-size: 12px;
        letter-spacing: 0;
    }

    .nav-profile-area {
        gap: 6px;
    }

    .nav-profile-icon {
        font-size: 20px;
    }

    .customer-name {
        font-size: 12px;
        max-width: 80px;
    }

    .logout-link {
        font-size: 12px;
    }
    
    .hamburger {
        display: flex;
    }
    
    .hero-title {
        font-size: 40px;
        letter-spacing: 2px;
    }
    
    .hero-subtitle {
        font-size: 16px;
    }
    
    .hero-buttons {
        flex-direction: column;
    }
    
    .hero-badges {
        flex-direction: column;
        align-items: center;
    }
    
    .page-header h1 {
        font-size: 36px;
    }
    
    .page-header p {
        font-size: 16px;
    }
    
    .section-title {
        font-size: 28px;
        margin-bottom: 40px;
    }
    
    .menu-grid {
        grid-template-columns: 1fr;
    }
    
    .reviews-grid {
        grid-template-columns: 1fr;
    }
    
    .rating-summary {
        flex-direction: column;
        text-align: center;
    }
    
    .footer-content {
        grid-template-columns: 1fr;
        text-align: center;
    }
    
    .footer-section p {
        justify-content: center;
    }
    
    .cart-drawer {
        width: 100%;
        right: -100%;
        height: 100vh;
        height: 100dvh;
        max-height: 100vh;
        max-height: 100dvh;
    }
    
    .cart-drawer.active {
        right: 0;
    }
    
    .mobile-sticky {
        display: flex;
    }
    
    .checkout-grid {
        grid-template-columns: 1fr;
    }
    
    .contact-grid {
        grid-template-columns: 1fr;
    }
    
    .about-content {
        grid-template-columns: 1fr;
    }
    
    .about-highlights {
        grid-template-columns: 1fr;
    }
    
    .services-grid {
        grid-template-columns: 1fr;
    }
    
    .menu-filters {
        flex-direction: column;
        gap: 16px;
    }
    
    .filter-categories {
        flex-wrap: wrap;
        justify-content: center;
    }

    /* ===== Mobile-only improvements (≤768px) ===== */

    /* Prevent horizontal overflow */
    html,
    body {
        overflow-x: hidden;
    }

    .navbar .container {
        flex-wrap: nowrap;
    }

    /* Touch-friendly targets (min 44px) */
    .btn,
    .nav-login-btn,
    .cart-btn,
    .hamburger,
    .add-to-cart,
    .quantity-btn,
    .filter-btn,
    .form-group input,
    .form-group select,
    .form-group textarea,
    .mobile-call,
    .mobile-order {
        min-height: 44px;
    }

    .cart-btn {
        padding: 10px;
    }

    .hamburger {
        padding: 10px;
    }

    /* Cart drawer — checkout stays visible & tappable */
    .cart-footer {
        padding-bottom: 20px;
    }

    .cart-footer .btn {
        min-height: 48px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .quantity-btn {
        width: 44px;
        height: 44px;
        font-size: 18px;
    }

    /* Checkout form — large, tap-friendly inputs */
    .checkout-form {
        padding: 24px 20px;
    }

    .checkout-form .form-group input,
    .checkout-form .form-group select,
    .checkout-form .form-group textarea {
        min-height: 48px;
        font-size: 16px;
    }

    .checkout-form .form-group textarea {
        min-height: 80px;
    }

    .checkout-form .btn-block {
        min-height: 50px;
        margin-bottom: 20px;
    }

    /* Menu cards — avoid cramped layout */
    .menu-card-content {
        padding: 20px 16px;
    }

    .add-to-cart {
        min-height: 48px;
    }

    /* Footer — reasonable spacing & touch targets */
    .footer-section ul li a {
        display: inline-block;
        padding: 8px 0;
        min-height: 44px;
        line-height: 1.4;
    }

    .footer-section p {
        padding: 6px 0;
    }
}

@media (max-width: 480px) {
    .container {
        padding: 0 16px;
    }
    
    .hero-title {
        font-size: 32px;
    }
    
    .hero-subtitle {
        font-size: 14px;
    }
    
    .btn {
        padding: 10px 20px;
        font-size: 14px;
    }
    
    .btn-lg {
        padding: 14px 28px;
        font-size: 16px;
    }
    
    .section-title {
        font-size: 24px;
    }
    
    .page-header {
        padding: 60px 0 40px;
    }
    
    .page-header h1 {
        font-size: 28px;
    }
    
    .promo-content h2 {
        font-size: 28px;
