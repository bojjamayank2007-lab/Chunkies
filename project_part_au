    overflow-x: hidden;
  }

  * {
    box-sizing: border-box;
  }

  img, svg, video, iframe, table {
    max-width: 100% !important;
    height: auto !important;
  }

  .container,
  .navbar .container,
  .menu-grid,
  .reviews-grid,
  .footer-content,
  .checkout-form,
  .order-summary,
  .cart-drawer,
  .cart-content,
  .about-highlights,
  .contact-grid,
  .contact-buttons {
    max-width: 100% !important;
    width: 100% !important;
  }

  .navbar,
  .navbar .container {
    flex-wrap: wrap;
  }

  .nav-menu {
    width: 100%;
    justify-content: flex-start;
  }

  .nav-actions {
    flex-wrap: wrap;
    justify-content: flex-end;
  }

  .btn,
  .nav-login-btn,
  .cart-btn,
  .hamburger {
    white-space: nowrap;
  }

  .cart-drawer {
    right: -100%;
    width: 100%;
    max-width: 100%;
  }

  .cart-content {
    width: 100%;
  }

  .cart-item {
    flex-wrap: wrap;
  }

  .cart-item-details {
    flex: 1 1 auto;
    min-width: 0;
  }

  .checkout-form,
  .order-summary,
  .checkout-section,
  .form-group {
    width: 100%;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
  }

  .footer-content {
    flex-direction: column;
  }

  .footer-section {
    width: 100%;
  }

  .menu-card,
  .review-card,
  .highlight,
  .service-card {
    width: 100% !important;
    max-width: 100%;
  }

  .filter-categories {
    flex-wrap: wrap;
  }

  .contact-buttons {
    flex-direction: column;
  }

  .contact-buttons .btn {
    width: 100%;
  }

  .modal-content {
    width: 90%;
    max-width: 90%;
  }

  .customer-orders-list .order-card {
    width: 100%;
    max-width: 100%;
  }

  .order-summary table,
  .order-summary * {
    max-width: 100%;
  }

  p, h1, h2, h3, h4, h5, h6, span, a {
    overflow-wrap: break-word;
    word-break: break-word;
  }
}

@media (max-width: 480px) {
  .navbar {
    padding: 10px 12px;
  }

  .nav-actions {
    gap: 4px;
  }

  .nav-login-btn {
    padding: 8px 10px;
    font-size: 12px;
  }

  .cart-btn,
  .hamburger {
    padding: 8px;
  }

  .menu-card-content {
    padding: 12px;
  }

  .checkout-form,
  .order-summary {
    padding: 16px;
  }

  .btn-block {
    width: 100%;
  }
}

@media (max-width: 768px) {
  /* Final containment pass */
  body, html {
    overflow-x: hidden !important;
  }

  section, main, footer, header, .container,
  .navbar, .nav-menu, .nav-actions,
  .hero, .menu-grid, .menu-card,
  .reviews-grid, .review-card,
  .about-highlights, .highlight,
  .contact-grid, .contact-buttons,
  .footer-content, .footer-section,
  .cart-drawer, .cart-content, .cart-item,
  .checkout-form, .checkout-section, .order-summary,
  .form-group, .form-row,
  .customer-orders-list, .order-card,
  .modal-content {
    max-width: 100vw !important;
    width: 100% !important;
  }

  .btn, .nav-login-btn, .cart-btn, .hamburger,
  .add-to-cart, .quantity-btn, .filter-btn {
    max-width: 100%;
    white-space: normal;
  }

  .cart-drawer {
    left: 0;
    right: 0;
    width: 100vw;
  }

  .navbar .container {
    flex-direction: column;
    align-items: stretch;
  }

  .nav-actions {
    justify-content: space-between;
    width: 100%;
  }

  .checkout-form .form-row {
    flex-direction: column;
  }

  .checkout-form .form-row > * {
    width: 100%;
  }
}

/* ============================================
   ULTIMATE MOBILE NAV + CART OVERLAY FIX
   ============================================ */
@media (max-width: 768px) {
  /* Force nav menu offscreen by default */
  html body #navMenu {
    position: fixed !important;
    top: 0 !important;
    left: -100% !important;
    right: auto !important;
    width: 80% !important;
    max-width: 300px !important;
    height: 100vh !important;
    display: flex !important;
    flex-direction: column !important;
    align-items: flex-start !important;
    gap: 16px !important;
    padding: 24px !important;
    margin: 0 !important;
    background: var(--bg-card, #141414) !important;
    transform: none !important;
    transition: left 0.3s ease !important;
    z-index: 2000 !important;
    overflow-y: auto !important;
  }

  html body #navMenu.active {
    left: 0 !important;
  }

  /* Ensure cart overlay is hidden unless active */
  html body .cart-overlay {
    display: none !important;
  }

  html body .cart-overlay.active {
    display: block !important;
  }

  /* Ensure cart drawer is offscreen unless active */
  html body .cart-drawer {
    transform: translateX(100%) !important;
  }

  html body .cart-drawer.active {
    transform: translateX(0) !important;
  }

  /* Keep hamburger visible */
  html body #hamburger {
    display: flex !important;
  }
}


==============================================
FILE: ./frontend/css/reviews.css
==============================================
/* Reviews Section */
.reviews {
    background-color: var(--bg-darker);
}

.rating-summary {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 32px;
    margin-bottom: 48px;
    padding: 32px;
    background-color: var(--bg-card);
    border-radius: 16px;
    border: 1px solid var(--border-color);
}

.rating-large {
    font-size: 64px;
    font-weight: 800;
    color: var(--primary-color);
}

.rating-total {
    font-size: 32px;
    color: var(--text-light);
}

.rating-info p {
    font-size: 18px;
    color: var(--text-light);
    margin-bottom: 8px;
}

.rating-info .stars {
    display: flex;
    gap: 4px;
    font-size: 24px;
    color: #ffc107;
}

.reviews-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 24px;
}

.review-card {
    background-color: var(--bg-card);
    padding: 24px;
    border-radius: 16px;
    border: 1px solid var(--border-color);
    transition: var(--transition);
}

.review-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow);
}

.review-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 16px;
}

.review-author {
    display: flex;
    align-items: center;
    gap: 12px;
}

.review-avatar {
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    font-weight: 700;
    color: var(--white);
}

.review-name {
    font-weight: 600;
    font-size: 16px;
}

.review-stars {
    display: flex;
    gap: 4px;
    color: #ffc107;
    font-size: 16px;
}

.review-text {
    color: var(--text-light);
    line-height: 1.6;
    font-style: italic;
}

.review-form {
    background-color: var(--bg-card);
    padding: 32px;
    border-radius: 16px;
    border: 1px solid var(--border-color);
    margin-top: 48px;
}

.review-form h3 {
    font-size: 24px;
    margin-bottom: 24px;
    text-align: center;
}

.rating-input {
    display: flex;
    gap: 8px;
    justify-content: center;
    margin-bottom: 24px;
}

.rating-input button {
    background: none;
    border: none;
    font-size: 32px;
    color: var(--text-light);
    cursor: pointer;
    transition: var(--transition);
}

.rating-input button:hover,
.rating-input button.active {
    color: #ffc107;
    transform: scale(1.2);
}

.review-form textarea {
    min-height: 120px;
    resize: vertical;
}

/* Very small screens */
@media (max-width: 320px) {
    .reviews-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 600px) {
    .rating-summary {
        flex-direction: column;
        gap: 16px;
        text-align: center;
    }
}

==============================================
FILE: ./frontend/css/style.css
==============================================
/* Reset and Base Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --primary-color: #ff6b35;
    --primary-dark: #e55a2b;
    --secondary-color: #1a1a1a;
    --text-color: #333;
    --text-light: #666;
    --bg-dark: #0f0f0f;
    --bg-darker: #080808;
    --bg-card: #1a1a1a;
    --bg-card-hover: #252525;
    --border-color: #333;
    --success-color: #4caf50;
    --error-color: #f44336;
    --warning-color: #ff9800;
    --white: #ffffff;
    --shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    --transition: all 0.3s ease;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: var(--bg-dark);
    color: var(--white);
    line-height: 1.6;
    overflow-x: hidden;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

/* Buttons */
.btn {
    display: inline-block;
    padding: 12px 28px;
    font-size: 16px;
    font-weight: 600;
    text-decoration: none;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: var(--transition);
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.btn-primary {
    background-color: var(--primary-color);
    color: var(--white);
