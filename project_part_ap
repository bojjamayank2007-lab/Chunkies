    font-weight: 700;
    margin-bottom: 8px;
}

.hero-card p {
    color: var(--text-light);
    font-size: 14px;
    margin-bottom: 16px;
}

.hero-card .price {
    font-size: 32px;
    font-weight: 800;
    color: var(--primary-color);
}

/* Promo Section */
.promo {
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
    padding: 60px 0;
    text-align: center;
}

.promo-content h2 {
    font-size: 48px;
    font-weight: 800;
    margin-bottom: 16px;
    text-transform: uppercase;
    letter-spacing: 2px;
}

.promo-content p {
    font-size: 20px;
    margin-bottom: 32px;
    color: rgba(255, 255, 255, 0.9);
}

.promo-content .btn {
    background-color: var(--white);
    color: var(--primary-color);
}

.promo-content .btn:hover {
    background-color: var(--bg-dark);
    color: var(--white);
}

/* Featured Menu */
.featured-menu {
    background-color: var(--bg-darker);
}

.menu-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 24px;
}

.menu-card {
    background-color: var(--bg-card);
    border-radius: 16px;
    overflow: hidden;
    border: 1px solid var(--border-color);
    transition: var(--transition);
}

.menu-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow);
    border-color: var(--primary-color);
}

.menu-card-image {
    width: 100%;
    height: 200px;
    background: linear-gradient(135deg, var(--bg-card-hover) 0%, var(--bg-dark) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 64px;
    color: var(--text-light);
    position: relative;
}

.menu-card-badge {
    position: absolute;
    top: 12px;
    left: 12px;
    background-color: var(--primary-color);
    color: var(--white);
    padding: 6px 12px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
}

.veg-indicator {
    position: absolute;
    top: 12px;
    right: 12px;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 12px;
    font-weight: 700;
}

.veg-indicator.veg {
    background-color: #4caf50;
    color: var(--white);
}

.veg-indicator.non-veg {
    background-color: #f44336;
    color: var(--white);
}

.menu-card-content {
    padding: 20px;
}

.menu-card-name {
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 8px;
}

.menu-card-description {
    color: var(--text-light);
    font-size: 14px;
    margin-bottom: 16px;
    line-height: 1.5;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.menu-card-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.menu-card-price {
    font-size: 24px;
    font-weight: 800;
    color: var(--primary-color);
}

.menu-card-rating {
    display: flex;
    align-items: center;
    gap: 4px;
    color: #ffc107;
    font-size: 14px;
}

.add-to-cart {
    background-color: var(--primary-color);
    color: var(--white);
    border: none;
    padding: 10px 20px;
    border-radius: 8px;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    font-size: 14px;
}

.add-to-cart:hover {
    background-color: var(--primary-dark);
    transform: scale(1.05);
}

@media (max-width: 360px) {
    .hero-card {
        max-width: 100%;
        padding: 24px;
    }
}

==============================================
FILE: ./frontend/css/menu.css
==============================================
/* Menu Grid & Cards
   (Used on menu.html, and also relied on by index.html's featured menu,
   about.html, contact.html, and cart.html, none of which load hero.css) */

.menu-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 24px;
}

.menu-card {
    background-color: var(--bg-card);
    border-radius: 16px;
    overflow: hidden;
    border: 1px solid var(--border-color);
    transition: var(--transition);
}

.menu-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow);
    border-color: var(--primary-color);
}

.menu-card-image {
    width: 100%;
    height: 200px;
    background: linear-gradient(135deg, var(--bg-card-hover) 0%, var(--bg-dark) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 64px;
    color: var(--text-light);
    position: relative;
    overflow: hidden;
}

.menu-card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.menu-card-badge {
    position: absolute;
    top: 12px;
    left: 12px;
    background-color: var(--primary-color);
    color: var(--white);
    padding: 6px 12px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    z-index: 1;
}

.veg-indicator {
    position: absolute;
    top: 12px;
    right: 12px;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 12px;
    font-weight: 700;
    z-index: 1;
}

.veg-indicator.veg {
    background-color: #4caf50;
    color: var(--white);
}

.veg-indicator.non-veg {
    background-color: #f44336;
    color: var(--white);
}

.menu-card-content {
    padding: 20px;
}

.menu-card-name {
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 8px;
}

.menu-card-description {
    color: var(--text-light);
    font-size: 14px;
    margin-bottom: 16px;
    line-height: 1.5;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.menu-card-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
}

.menu-card-price {
    font-size: 24px;
    font-weight: 800;
    color: var(--primary-color);
}

.menu-card-rating {
    display: flex;
    align-items: center;
    gap: 4px;
    color: #ffc107;
    font-size: 14px;
}

.add-to-cart {
    width: 100%;
    background-color: var(--primary-color);
    color: var(--white);
    border: none;
    padding: 12px 20px;
    border-radius: 8px;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.add-to-cart:hover {
    background-color: var(--primary-dark);
    transform: scale(1.02);
}

.empty-cart {
    text-align: center;
    padding: 40px;
    color: var(--text-light);
    font-size: 16px;
    grid-column: 1 / -1;
}

@media (max-width: 320px) {
    .menu-grid {
        grid-template-columns: 1fr;
    }
}

==============================================
FILE: ./frontend/css/mobile-app.css
==============================================
/* =====================================================
   CHUNKIES MOBILE APP-LIKE UI OVERRIDE
   Applies only to screens ≤ 768px.
   Desktop/laptop layout remains untouched.
   ===================================================== */

@media (max-width: 768px) {

  /* Prevent horizontal scroll globally */
  html, body {
    overflow-x: hidden !important;
    max-width: 100% !important;
  }

  /* Add space for bottom nav */
  body {
    padding-bottom: 72px !important;
  }

  /* Navbar: clean flex layout — brand left, nav actions right.
     No absolute positioning and no forced heights; base sizing
     from navbar.css is left untouched. (Overrides responsive.css's
     stacked mobile fallback for the container.) */
  .navbar .container {
    flex-direction: row !important;
    flex-wrap: nowrap !important;
    align-items: center !important;
    justify-content: space-between !important;
    width: 100% !important;
    max-width: 100% !important;
    padding: 0 12px !important;
  }

  .nav-brand {
    flex: 0 1 auto !important;
    min-width: 0 !important;
    max-width: 55% !important;
    display: flex !important;
    flex-direction: column !important;
  }

  .nav-brand a {
    font-size: 20px !important;
    white-space: nowrap !important;
    overflow: hidden !important;
    text-overflow: ellipsis !important;
  }

  /* Hamburger — mobile only, sits naturally at the end of
     .nav-actions (right side). No absolute positioning. */
  #hamburger {
    display: flex !important;
    flex-direction: column !important;
    justify-content: center !important;
    align-items: center !important;
    gap: 5px !important;
    padding: 10px !important;
    min-height: 44px !important;
    min-width: 44px !important;
  }

  /* Slide-in nav menu */
  html body #navMenu {
    position: fixed !important;
    top: 0 !important;
    left: -100% !important;
    right: auto !important;
    width: 80% !important;
    max-width: 320px !important;
    height: 100vh !important;
    display: flex !important;
    flex-direction: column !important;
    align-items: flex-start !important;
    gap: 20px !important;
    padding: 24px !important;
    margin: 0 !important;
    background: #0d0d0d !important;
    transform: none !important;
    transition: left 0.3s ease !important;
    z-index: 3300 !important;
    overflow-y: auto !important;
  }

  html body #navMenu.active {
    left: 0 !important;
  }

  html body #navMenu li {
    width: 100% !important;
    max-width: 100% !important;
    list-style: none !important;
    margin: 0 !important;
  }

  html body #navMenu li a {
    display: block !important;
    width: 100% !important;
    font-size: 18px !important;
    padding: 12px 0 !important;
    color: #fff !important;
    text-decoration: none !important;
  }

  /* Nav actions area — right-aligned group (login/profile, cart, hamburger) */
  .nav-actions {
    display: flex !important;
    flex-direction: row !important;
    align-items: center !important;
    gap: 8px !important;
    flex: 0 0 auto !important;
    margin-left: auto !important;
  }

  .nav-actions > * {
    margin: 0 !important;
  }

  /* Hide old bottom sticky call/order bar */
  .mobile-sticky,
  .mobile-call,
  .mobile-order {
    display: none !important;
  }

  /* Cart drawer full-width */
  html body .cart-drawer {
    position: fixed !important;
    top: 0 !important;
    right: -100% !important;
    left: auto !important;
    width: 100% !important;
    max-width: 100% !important;
    height: 100vh !important;
    transform: none !important;
    transition: right 0.3s ease !important;
    z-index: 4000 !important;
    background: #141414 !important;
  }

  html body .cart-drawer.active {
    right: 0 !important;
  }

  html body .cart-overlay {
    display: none !important;
    position: fixed !important;
    inset: 0 !important;
    background: rgba(0,0,0,0.6) !important;
    z-index: 3900 !important;
